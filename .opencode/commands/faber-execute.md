---
description: Dispatch a Faber task to execute a PLAN.md
agent: smart
---

Load the `running-faber-tasks` skill, then dispatch a Faber task with `faber run --model smart` and a prompt that tells the agent to load the `executing-work` skill and follow it to implement $ARGUMENTS.
